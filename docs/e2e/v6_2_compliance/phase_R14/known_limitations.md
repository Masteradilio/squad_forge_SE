# Phase R14 Known Limitations

- The configured Chief Engineer provider returned HTTP 402 because the
  OpenRouter account has insufficient credits.
- No product code was generated by the accepted run, so no PR or visual/function
  acceptance conclusion is valid.
- The preflight deliberately does not substitute a local model for the Chief;
  doing so would invalidate the API-led V6 acceptance gate.
